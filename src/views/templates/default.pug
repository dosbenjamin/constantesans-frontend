block setup
  - const description = process.env.PROJECT_DESCRIPTION

- const name = `${title} - ${process.env.PROJECT_NAME} [MVP]`
- const url = process.env.PROJECT_URL
- const coverPath = ''
- const cover = `${url}/${coverPath}`

<!DOCTYPE html>
html(lang='fr' itemscope)

  include ../layouts/head.pug

  body(data-namespace=page)
    block header

    main
      //- block content

      block modalArea
        each content, id in modals
          include ../components/modal/modal.pug

    block footer
